template_code = """
#include <stdlib.h>
#include <string.h>
#include <stdio.h>

/*
* Демонстрация форматированного вывода в строку:
* 1. Выделение памяти под строку определенного размера
* 2. Форматирование с помощью sprintf()
* 3. Проверка результата форматирования
* 
* Особенности:
* - sprintf автоматически добавляет нуль-терминатор
* - Необходимо правильно рассчитать размер буфера
* - Формат строка "%s %s!" подразумевает два строковых параметра
*/

int main() {
    // Выделяем память для форматированной строки
    char *string1 = malloc($$len_string_8$$);
    
    // Форматируем строку из двух параметров
    sprintf(string1, "%s %s!", $$format_8_1$$, $$format_8_2$$);
    
    // Проверяем результат форматирования
    if (strcmp({[1]}, string1) != 0)
        return 1;
    
    // Освобождаем память
    free(string1);
    return 0;
}
"""

template_condition = "Заполните пропущенные значения переменных таким образом, чтобы программа завершалась с кодом 0."

ban_words = ["exit", "return", "[", "goto"]

[[error_messages]]
"1" = "Форматированная строка не соответствует ожидаемому результату"
"2" = "Недостаточный размер выделенной памяти"
