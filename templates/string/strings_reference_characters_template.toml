template_code = """
#include <stdlib.h>
#include <string.h>
#include <stdio.h>

/*
* Различные способы доступа к символам строки:
* 1. Прямая индексация через []
* 2. Доступ через арифметику указателей
* 3. Сравнение значений символов
* 
* Особенности:
* - Все операции с символами используют ASCII-коды
* - Указательная арифметика учитывает размер типа char
* - Можно комбинировать разные способы доступа
*/

int main() {
    // Инициализация строки
    char *string = $$string_2$$;
        
    // Проверка символа по индексу
    if ({[1]} != string[$$index_2_1$$])
        return 1;
    
    // Проверка через комбинированный доступ
    if ({[2]} != (string + $$index_2_2$$)[$$index_2_3$$])
        return 1;
    
    // Сравнение символьных значений
    if (*string + $$index_2_4$$ {[3]} *(string + $$index_2_4$$))
        return 1;

    return 0;
}
"""

template_condition = "Заполните пропущенные значения переменных таким образом, чтобы программа завершалась с кодом 0."


ban_words = ["exit", "return", "[", "goto"]


[[error_messages]]
"1" = "Неверный символ при обращении по индексу"
"2" = "Ошибка при работе с арифметикой указателей"
"3" = "Некорректное сравнение символов"
